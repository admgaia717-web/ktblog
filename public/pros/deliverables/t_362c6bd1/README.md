# t_362c6bd1 — a / Annie Apple 進捗README（課金ゼロ先行パス完了時点）

更新: 2026-09-04 13:15 JST / 作業者: eundong / 上位: t_7ea0884b（root）
正本パラメータ: `~/ProsLC/deliverables/t_95c471e2/params.md`（オ課長承認済・変更禁止・実行時そのまま使用）

## 1. 納品物（本時点）

| ファイル | 内容 | 状態 |
|---|---|---|
| `still/a_annie_still_final.png` | **正式キャラ静止画**（v5bの背景a文字を除去した完成版。1344x768） | 検品合格 |
| `still/a_annie_still_v1..v8*.png` | 試行履歴 8版（下記 §3） | 参考 |
| `storyboard/storyboard_a.md` | 絵コンテ・アクション分解・音設計・実行パラメータ参照 | 完成 |
| `a_annie_apple.mp4` | 本編動画 | **パスB仮初版あり・要差替**（下記 §2） |

## 2. 動画生成の現状と再開条件

### 2.1 既存パスB仮初版（2026-09-04 10:35-10:46・別セッション実行分）

`fal_usage_log.md` によると本日10時台に kling **v2.5-turbo std i2v**（5秒・音声オフ）が2テイク実行済み
（take1 QA失敗: 口へ運ぶアクションなし → take2。推定計 $0.42≒63円・**§1の本番モデル v2.6 std motion-control からの代替**）。
残存ファイル: `a_annie_apple_raw.mp4`(take1) / `a_annie_apple_raw_take2.mp4`(take2) /
`a_annie_apple_loop3_preview.mp4`(12.5s ループ試作・音声付き) / `a_annie_apple.mp4`(4.67s=take2トリム) /
`a_annie_apple_sound.mp4`(4.14s・音声合成？) / `a_sound_aah.ogg`(2.6s)

**注意点**（オ課長 12:35 確認済みの運用方針を反映）:
- take2 が**採用済み**（オ課長 2026-09-04 12:35 指示）。4.67秒は仕様 5〜11秒 下限未満だが、**本番差替まで現状維持**。5.05秒ループ版の組立スクリプト提案を `make_loop_v1.sh`（作業ワークスペース）に退避
- 音声: `a_sound_aah.ogg` は**TTS仮**（オ課長確認）。本番は実音響 /a/ に差し替え（TTS禁止方針の最終形は本番テイクで適用）
- 課金: take1+take2 計2テイク（fal_usage_log.md 記録・64円/テイク表記）

### 2.2 正本ルート（未実施・オ課長指示により当カードの作業はここに一本化）

- KT元動画 `t_5032b91d/source/mark_a_annie_apple.mp4`: **未着**（期限 9/10）
- 着後 → 標準フロー（gen_a.py 的ランナー → テイク記録 → ループ編集）で motion-control 出力に差替（params.md §2/§4a 準拠・1テイク $0.56≒84円・上限3テイク）→ **これが最終形**
- b・c は KT元動画待ち（blocked・前工程完了・課金0円）。9/12 09:00 未着なら**オ課長がパスB（3本 i2v 先行）を発動**

### 2.3 運用決定記録（オ課長 2026-09-04 12:35 JST）

1. SNS下書き7案が参照する静止画は **v5b_retouched に固定**（差替不要）
2. **v6〜v8 の追加生成は停止**（検品記録のない素材の増殖防止）。次に still を触るのは本番 motion-control テイク1本目確定後
3. 当カードの残作業は「**元動画到着後の本番生成**」に一本化

## 3. 静止画試行サマリ（vision検品結果）

| 版 | 結果 |
|---|---|
| v1 | 黄色い卵型キャラ・デザイン不一致 |
| v2 | 赤リンゴ化したが左腕欠け・額隆起 |
| v3 | キャラ良品。ただし背景左に浮いた「a」文字が混入 |
| v4 | aフォルム化したが顔消失・体に穴 |
| v5a | 顔・背景良・a尻尾なし |
| **v5b** | **キャラ完全（顔・両手・小リンゴ・足・口）**。背景に「a」→ 行内補間で除去 → `a_annie_still_final.png` |
| v6/v6b | 良質だがaの尻尾フック出ず |
| v7/v8(SDXL) | 尻尾出ず/v8は多リンゴ破綻 |

技術メモ: 「丸いリンゴ体＋小文字a融合」は flux1-dev / SDXL とも事前学習バイアスで直接生成困難（8版検証）。
a融合はテンプレート（jumeok 9/12）の「文字表記」指定を受けて構図（例: 大きなaの中にキャラ配置）で解決するのが妥当。

## 4. 次のアクション

1. KT元動画着（9/10期限）→ 様式確認（t_5032b91d §2）→ motion-control 実行（テイク記録は本READMEに追記）
2. 9/12 判定: 未着ならパスB（i2v・1テイク）を `a_annie_still_final.png` で実行
3. chisu 検品（t_631e569b）→ 9/16 18:00 確定
